<%# Page for editing an existing bookmark — same shared _form as "new";
    form_with sees a saved record and submits as PATCH automatically. %>
<h1>Edit bookmark</h1>

<%= render "form", bookmark: @bookmark %>

<%= link_to "Back to bookmarks", root_path %>
